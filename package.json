{
  "name": "hojapersonaje-coc7",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "next dev",
    "dev:poll": "WATCHPACK_POLLING=true next dev",
    "dev:clean": "rm -rf .next tsconfig.tsbuildinfo && next dev",
    "build": "next build",
    "build:clean": "rm -rf .next tsconfig.tsbuildinfo && next build",
    "prestart": "node scripts/ensure-next-build.mjs",
    "start": "next start",
    "lint": "next lint",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "next": "^15.5.12",
    "pdf-lib": "^1.17.1",
    "react": "19.0.0",
    "react-dom": "19.0.0",
    "zod": "^3.24.1",
    "zustand": "^5.0.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.1",
    "@types/node": "^24.7.2",
    "@types/react": "^19.0.5",
    "@types/react-dom": "^19.0.2",
    "jsdom": "^28.1.0",
    "typescript": "^5.7.3",
    "vitest": "^4.0.18"
  },
  "msw": {
    "workerDirectory": [
      "public"
    ]
  }
}
